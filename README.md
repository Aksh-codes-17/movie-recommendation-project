The Movie Recommendation System is a simple project that helps users discover movies based on their interests. The system uses a movie dataset that contains information such as movie title, genre, rating, and overview. When a user searches or selects a movie, the system suggests similar movies using basic filtering or similarity methods.

This project is developed using HTML and dataset is stored in a csv file.
